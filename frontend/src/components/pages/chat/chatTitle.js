const ChatTitle = () => {
    const chatTitleStyle = {
        color: "#C72E2E",
        fontWeight: "bold",
        textAlign: "center",
    };

    return (
        <>
            <div>
                <h1 style={chatTitleStyle}>Live Chat: (RED LINE)</h1>
            </div>
        </>
    );
};

export default ChatTitle;
