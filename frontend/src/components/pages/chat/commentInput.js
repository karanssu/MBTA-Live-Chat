const CommentInput = () => {
    const inputStyle = {
        width: "100%",
        height: "50px",
    };

    return (
        <>
            <input style={inputStyle}></input>
        </>
    );
};

export default CommentInput;
